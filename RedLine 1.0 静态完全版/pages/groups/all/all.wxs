var foo = "'hello world' from tools.wxs";

var judge = function (member, value) {
  if (member.indexOf(value) >= 0)
    return true;
  else
    return false;
}

module.exports = {
  FOO: foo,
  judge: judge,
};